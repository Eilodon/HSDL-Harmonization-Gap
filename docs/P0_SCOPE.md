# P0 scope boundary

This pull request implements the reusable engineering substrate only. It does not:

- re-encode or promote the provisional current-law graph;
- generate current-law directional percentages;
- change the independent-review gate;
- alter the frozen legacy policy bundle;
- claim external HSDL compatibility;
- implement symbolic typed-region refinement.

Those changes depend on this foundation and belong in subsequent focused pull requests.
