[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "hsdl-harmonization-gap"
version = "0.1.0"
description = "Canonical, typed reference model for the HSDL harmonization-gap research program"
requires-python = ">=3.11"
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]
